# 分类大厅扩展记录

## 已落地

- 8 个常驻分类，16 个常驻辩题，前四类各 3 题，后四类各 1 题；另有 1 个从当日热榜挑出的 featured 今日热辩。
- 保留 parenting / career 与 family / work 旧链接。
- 14 个新增题目与今日热辩均通过官方 CLI 搜索，选取有署名、原句可匹配的 6 条观点，编排成 3 个回合。语境、条件与跨问题差异明确展示，不构造“真实作者说过”的新发言。
- 今日热辩作为完整辩题进入三回合现场；大厅不再展示未编排的热榜外链卡，也没有虚构热度或投票数。
- 增加最近新增和紧凑分类卡；保留红蓝配色、手机布局和键盘焦点。

## 验证与限制

- 自动测试 32 项通过；覆盖全部新增题目的直接打开、真实引句展示、三回合回顾、回大厅；分类筛选、缓存、失败冷却和迟到请求隔离。
- 对所有新增题目的实际 API 返回进行了原句匹配回归；来源改变时不补位，失效回合不冒充已核对席位。
- 运行中服务的最终批量联调：aa、partner 返回正常 1 对 1；随后上游返回 Code 30001，服务显示 HTTP 429，已停止搜索重试。不能把这次批量联调称为全量通过；其余来源的证据来自本轮前面的逐题官方搜索及对应离线回归。
- `/api/hot` 曾独立联调正常，今日热辩使用其中的“胖东来四年学员制”话题作为 featured 题目。运行中的 `/api/topics` 已确认 8 类、16 个常驻题与 1 个 featured 题。
- 浏览器控制返回 `nodeRepl.fetch request failed`，无法完成实际截图和像素级布局验收。DOM 交互测试不替代视觉验收。
- 实时 AI 质询不在此轮范围，仍明确标记未接入。
