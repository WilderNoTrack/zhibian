# 复制这个文件为 .env 再填写。.env 已被 .gitignore 忽略，不会进入仓库，
# 也不会出现在接口响应或浏览器里。
#
# 服务启动时会自动读取项目根目录的 .env；已经存在的系统环境变量优先，
# 不会被这里的值覆盖。测试不会读取这个文件。

# 必填：DeepSeek 密钥
DEEPSEEK_API_KEY=

# 可选：默认 https://api.deepseek.com
DEEPSEEK_BASE_URL=

# 可选：调试时打印服务端错误对象
# ZHIBIAN_DEBUG=1
